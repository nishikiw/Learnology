describe('testing edit controller', function() {

	var scope; 
	var controller; 

	beforeEach(module('learnologyApp')); 
	
	beforeEach(inject(function($rootScope, $controller) {
		scope = $rootScope.$new(); 
		controller = $controller('save', 

});
